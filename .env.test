NODE_ENV=test
PORT=3001
SESSION_STORAGE_PATH=/tmp/exelearning-sessions
TEMP_STORAGE_PATH=/tmp/exelearning-temp
# Bun loads .env.test after .env, so this is the override point for values .env already sets (e.g. DB_PATH).
DB_PATH=:memory:

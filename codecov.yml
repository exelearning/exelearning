coverage:
  status:
    project:
      default:
        target: 80%
    patch:
      default:
        target: 80%

flags:
  unittests:
    paths:
      - src/
  frontend:
    paths:
      - public/app/
      - public/files/perm/idevices/base/
      - public/libs/

ignore:
  # Vendored third-party code (never edited locally, see THIRD-PARTY-NOTICES.md)
  - public/app/common/scorm/scorm12/vendor/**

